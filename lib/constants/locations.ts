/**
 * Country name exactly as the [Countries Now](https://countriesnow.space) API expects.
 * Change this string to load states and cities/LGAs for another country (for example
 * `"Ghana"` or `"Kenya"`). Phone validation and UI copy may still assume Nigeria until
 * updated separately.
 */
export const LOCATIONS_API_COUNTRY_NAME = "Nigeria" as const;
